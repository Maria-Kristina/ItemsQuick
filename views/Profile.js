import React from 'react';
import {Container, Text, Header} from 'native-base';

const Profile = (props) => {
return (
    <Container>
        <Header>
            <Text>Profile</Text>
        </Header>
    </Container>
    );
};

export default Profile;
