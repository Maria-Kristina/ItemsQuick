import React from 'react';
import {Container, Text, Header} from 'native-base';

//User profile page

const Profile = (props) => {
return (
    <Container>
        <Header>
            <Text>Profile</Text>
        </Header>
    </Container>
    );
};

export default Profile;
