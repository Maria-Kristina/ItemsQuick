import React from 'react';
import {Container, Text, Header} from 'native-base';

// Media upload page

const Upload = () => {
    return(
        <Container>
            <Header>
                <Text>
                    Post upload page
                </Text>
            </Header>
        </Container>
    );
};

export default Upload;
